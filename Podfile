# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ToDoList' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ToDoList

  target 'ToDoListTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ToDoListUITests' do
    # Pods for testing
  end

  # Add the following line to specify the platform
  platform :ios, '13.0' # Adjust the iOS version as per your project's requirements

  # Add the pod for FSCalendar
  # pod 'FSCalendar'
end
